import React from 'react';

const instanceId = (props) => {
    return (
        <div className="InstanceID">          
            <p>{props.instance_id}</p>           
        </div>    
    )
};

export default instanceId;